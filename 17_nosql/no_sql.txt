# Traer 3 restaurantes que hayan recibido al menos una calificación de grade 'A' con score mayor a 50. Una misma calificación debe cumplir con ambas condiciones simultáneamente; investigar el operador elemMatch.

1. db.restaurants.find({grades : {$elemMatch : {grade : "A" , score : {$gt : 50}}}}).pretty()

# ¿A cuántos documentos les faltan las coordenadas geográficas? En otras palabras, revisar si el tamaño de address.coord es 0 y contar.

2.db.restaurants.count({"address.coord" : {$size:0}})

# Devolver name, borough, cuisine y grades para los primeros 3 restaurantes; de cada documento solo la última calificación. Ver el operador slice.

3.db.restaurants.find({} , {name:1 ,borough:1 ,cuisine:1,grades:{$slice :-1}}).limit(3).pretty()