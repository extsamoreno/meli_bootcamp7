package com.example.demo.project.excercise1.service.mapper;

import com.example.demo.project.excercise1.repository.entities.Plato;
import com.example.demo.project.excercise1.service.dto.PlatoDTO;

public class PlatoMapper { // ¿Implementa platoDTO?

    public static PlatoDTO toDTO(Plato plato) {
        //return new PlatoDTO(plato.getName(), plato.getIngredientes());
        //return new IngredienteDTO(plato.getName(), plato.getCalories());
        return null;
    }
}
