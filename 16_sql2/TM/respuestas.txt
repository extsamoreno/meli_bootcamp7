Primera Parte

1. A qué se denomina JOIN en una base datos?
Es la combinación de datos de una tabla con datos de la otra tabla a partir de una o varias condiciones en común.

2. Nombre y explique 2 tipos de JOIN.
INNER JOIN: combinar cada fila de una tabla con cada fila de la otra tabla, seleccionado aquellas filas que cumplan una determinada condición
LEFT JOIN: combina los valores de la primera tabla con los valores de la segunda tabla, siempre va a devolver las filas de la primera tabla, incluso aunque no cumplan la condición

3. Para qué se utiliza el GROUP BY?
Se utiliza para agrupar los resultados según las columnas indicadas.

4. Para qué se utiliza el HAVING?
Se utiliza para filtrar una consulta, como el WHERE, pero afecta a los grupos obtenidos por el GROUP BY y no a las filas de la tabla.

5. Dado lo siguientes diagramas indique a qué tipo de JOIN corresponde cada uno:Diagrama 1: INNER JOIN
Diagrama 2: LEFT JOIN6. Escriba una consulta genérica por cada uno de los diagramas a continuación:
Diagrama 1: RIGHT JOIN
Consulta:
SELECT *
  FROM tabla1 a
 RIGHT JOIN tabla2 b
    ON a.id = b.id

Diagrama 2: FULL JOIN
Consulta: 
SELECT *
  FROM tabla1 a
  FULL OUTER JOIN tabla2 b
    ON a.id = b.id
